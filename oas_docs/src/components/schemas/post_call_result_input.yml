---
components:
  schemas:
    PostCallResultInput:
      required:
      - id
      properties:
        id:
          description: Result id
          type: integer
      xml:
        name: PostCallResultInput
