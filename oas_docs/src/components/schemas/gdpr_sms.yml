---
components:
  schemas:
    GdprSms:
      properties:
        created_at:
          type: string
          format: date-time
        receiver:
          type: string
        sender:
          type: string
        content:
          type: string
      xml:
        name: GdprSms
