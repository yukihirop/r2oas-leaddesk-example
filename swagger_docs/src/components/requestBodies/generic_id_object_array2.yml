---
components:
  requestBodies:
    genericIdObjectArray2:
      content:
        application/json:
          schema:
            property: product_groups
            type: array
            items:
              "$ref": "#/components/schemas/genericIdObject"
      description: Request body comprising of objects each containing a product group
        id
